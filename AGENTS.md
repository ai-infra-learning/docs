# AI Infra 学习社区文档项目

## About this project

- 这是一个 AI Infra 学习社区网站，基于 [Mintlify](https://mintlify.com) 构建
- 网站包含三大板块：**在线分享**、**读书会**、**学习资源**
- 页面使用 MDX 格式，带 YAML frontmatter
- 配置文件为 `docs.json`
- 运行 `mint dev` 本地预览
- 运行 `mint broken-links` 检查链接

## Site structure

- `index.mdx` — 社区首页
- `about.mdx` — 关于社区
- `how-to-join.mdx` — 如何参与
- `talks/` — 在线分享
  - `index.mdx` — 分享概览
  - `01-vllm-quickstart.mdx` — 各期分享页面
- `books/<book-slug>/` — 读书会，每本书一个目录
  - `index.mdx` — 书籍概览
  - `schedule.mdx` — 阅读计划
  - `chapters/ch01.mdx` — 各章节笔记
  - `resources.mdx` — 延伸资源
- `resources/` — 学习资源
  - `index.mdx` — 资料概览
  - `llm-inference.mdx` — 按主题分类的资料页面

## Adding a new talk

1. 在 `talks/` 下创建新的 MDX 文件，如 `talks/08-new-topic.mdx`
2. 在 `docs.json` 的 `navigation.tabs` → "在线分享" tab 中添加页面路径
3. 更新 `talks/index.mdx` 的卡片列表

## Adding a new book

1. 在 `books/` 下创建新目录，如 `books/new-book/`
2. 创建 `index.mdx`、`schedule.mdx`、`resources.mdx` 和 `chapters/` 目录
3. 在 `docs.json` 的 `navigation.tabs` → "读书会" tab 的 `dropdowns` 数组中添加新的 dropdown 项

## Adding a new resource topic

1. 在 `resources/` 下创建新的 MDX 文件，如 `resources/new-topic.mdx`
2. 在 `docs.json` 的 `navigation.tabs` → "学习资源" tab 中添加页面路径
3. 更新 `resources/index.mdx` 的卡片列表

## Writing Preferences

以下规则适用于仓库中的技术文章、教程、说明文档和页面文案。除非用户明确提出其他要求，否则按这些规则写作和修改。

### 资料与准确性

- 权威资料已有准确、简洁的定义时，优先采用原文，不要为了换一种说法而降低准确度。
- 优先使用官方文档、规范、源码和可复现的实验结果。明确区分来源事实、实验结果、工程判断和推测。
- 引用文字、图片或结论时标明来源。来源放在图注、段落附近或页面末尾的相关资料中，不添加打断正文的重复声明。
- 添加互联网资料时，在首次提及处或关键表格中添加直接链接，不只在页面末尾集中列出。
- 版本、默认行为、兼容性和性能结论可能变化，写入前应检查当前资料或实际环境。
- 不为增强语气而扩大结论。说明结论成立的版本、环境和适用范围。

### 开头与结构

- 直接从事实、问题或结论开始。不要刻意添加故事、悬念或“先看一个例子”式引子。
- 每页采用倒金字塔结构：先写最重要的信息，再解释机制、实现细节、证据和扩展内容。
- 一个小节只承担一个主题。独立的组件、阶段或机制分开介绍，再说明它们之间的关系。
- 先介绍单个概念和组件，最后再用端到端流程串联；不要在概念尚未讲清楚时提前混合多个主题。
- 删除不推进主线、不给出新信息的段落。
- 避免关于文章或章节结构的元评论，直接讨论主题本身。

### 标题与链接

- 标题使用简短、明确、内容优先的名词短语或陈述句。
- 标题不使用数字前缀，也不写成疑问句。
- 避免“为什么”“看懂”“揭秘”“如何理解”“有什么”“这一章”“这里”“下面”“先理解”“记住”“你要”“你该”等模板化标题或表格标签。
- 链接到其他页面时，使用目标页面的完整标题，不使用数字编号或含义不清的简称。
- 只有确实存在先后依赖的操作步骤才使用编号列表。

### 语言与语气

- 中文内容为主，使用自然、直接、克制的中文，保持技术文章应有的准确度。
- 保持句子简洁，采用公开技术文章或博客的语气，避免讲义式和导读式表达。
- 优先使用主动语态。
- 可以在自然的语境中使用第二人称，但避免“你要”“你该”“先记住”等教学式表达。
- 每句话都应提供事实、机制、原因、限制、证据或必要背景。
- 优先使用肯定句描述系统实际如何工作，写清主体、动作、对象和结果。能写成“谁通过什么完成什么”时，不通过“不会做什么”“不是怎样工作”间接说明机制。
- 只有“不支持”“禁止”“不会发生”本身是关键限制时，才使用否定句。
- 删除常识性废话、重复总结、替读者下结论的句子和机械过渡。
- 可以用“下图展示了……”“下表列出了……”指向紧随其后的图表，并直接概括图表内容；避免“下面来看”“如下所示”等不提供信息的过渡。
- 避免修辞性“为什么”句式，直接陈述结论和原因。
- 避免翻译腔、宣传腔和 AI 模板句，包括但不限于：
  - “可以概括为一句话”
  - “围绕……展开”
  - “值得注意的是”
  - “这带来了一个重要结果”
  - “这意味着……”
  - “不仅……而且……”
  - “让我们先来看看……”
- 不连续使用“首先、其次、最后”组织段落。让内容按照事实和因果关系自然推进。
- 专业术语使用业界通行写法，并在全文保持名称、大小写和译法一致。
- 文件名、命令和路径使用代码格式。

### 信息密度

- 内容可以详细，但深度应来自机制、调用关系、数据流、限制条件和真实证据，不来自形容词、重复解释或列表堆砌。
- 不写显而易见的组件边界。例如已经说明某组件负责调度时，无需再补充它“不负责创建容器”。
- 简介只保留最关键的职责和动作。详细内容放入对应章节，不在总览中重复展开。
- 表格只用于需要逐项比较的信息；两个简单事实可以直接写成句子。

### 图片、代码与实验

- 官方已有清晰图片时，优先使用官方图片并注明来源。条件允许时将图片保存到仓库，避免依赖外部热链。
- Mermaid 只用于官方资料没有覆盖的特定流程、关系或实验结果，不重复绘制已有的官方架构图。
- 代码、命令和配置必须帮助解释正文。不要为了让页面显得完整而强行添加示例。
- 完整配置、一键脚本和验证流程可以集中放在实验页面；概念页面只保留理解机制所需的最小示例。
- 实验结论应附带可复现命令、日志、对象状态或其他证据，不把未执行的示例写成验证结果。

### 交付前检查

- 开头是否直接说明本页最重要的信息。
- 每个小节是否只讨论一个主题。
- 是否存在可删除而不损失信息的句子。
- 标题是否简短、无编号、非疑问句。
- 跨页链接是否使用完整标题。
- 术语、大小写和版本是否一致。
- 引用、图片和结论是否有可靠来源。
- 代码和实验是否与正文一致并经过必要验证。
- 通读一遍，确认文字像技术作者写作，而不是模板生成的摘要。
